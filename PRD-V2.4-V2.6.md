# 格致 · 科研工作台 V2.4–V2.6 产品需求文档（回顾统计 · 文献线索 · 会议纪要 · 写作度量）

| 项目 | 内容 |
| --- | --- |
| 文档版本 | V1.0 |
| 发布日期 | 2026-08-22 |
| 状态 | 已定稿，全部决策已确认，待启动开发 |
| 覆盖范围 | V2.4 / V2.5 / V2.6 三个版本 |
| 前置文档 | [PRD.md](PRD.md)（V1–V2.3，已冻结为历史定稿；本 PRD 对其部分决策的修订仅在本文档内声明生效） |
| 产品名称 | 格致（Gezhi）· 科研工作台（`gezhi-workbench`） |
| 产品形态 | Windows 桌面应用（单用户，本地明文 JSON 存储，零服务器） |
| 开发方式 | AI 辅助开发，产品所有者提需求并验收 |
| 评审人 | 产品所有者（硕士研究生本人） |

---

## 1. 概述与定位

V1–V2.3 已完成「项目 → 任务 → 论文 → 成果」的核心管理闭环。本 PRD 定义下一阶段的三个版本（V2.4–V2.6），共四个功能方向：

| 方向 | 一句话定位 | 落入版本 |
| --- | --- | --- |
| 回顾与统计 | 把已经在本地 JSON 里的数据变成可决策的洞察（投稿周期、项目投入、年度回顾） | V2.4 |
| 进展日志模板 | 让「实验记录、文献笔记、调试排错」有结构化骨架，降低记录成本 | V2.4 |
| 文献线索层 | 从 Zotero 导入文献条目，管理阅读状态，与灵感/任务/论文串联，一键复制引用格式 | V2.5 |
| 会议纪要与写作度量 | 导师会议行动项落实为任务；写作计时与字数记录汇入周报 | V2.6 |

**竞品调研结论**（2026-08 调研，详见第 2 节）：市面上没有任何产品把「项目/任务/投稿流转/成果台账 + local-first」做成一站式——Zotero 管文献不管流程，Overleaf 管写作不管项目，ELN 管记录不管成果，Notion/Obsidian 靠模板拼装。这是格致的差异化空档。

**本 PRD 的总原则**：每个新功能必须回答「这条信息如何串进 *项目 → 任务 → 论文 → 成果* 主链」。串得上的做深；串不上的（PDF 阅读、协作、引文图谱）交给生态里的其他工具，格致只通过导入/导出与它们握手。这也是 Zotero / Obsidian / JabRef 等 local-first 长青产品共同的活法：**核心克制、边界开放**。

## 2. 背景与竞品调研结论

### 2.1 调研摘要

| 品类 | 代表产品 | 核心能力 | 与格致的关系 |
| --- | --- | --- | --- |
| 文献管理/阅读 | Zotero 7、JabRef、ReadCube、知云/小绿鲸 | PDF 内注释回链、标签体系、citekey/BibTeX、划词翻译 | 阅读体验已饱和，不做；但「文献状态与科研流程串联」是空档 |
| 学术写作 | Overleaf、Typst、Zettlr | 版本历史、字数统计、修订追踪 | 写作本身不进入；写作**度量**（时长/字数）是 thesis tracker 品类验证过的轻需求 |
| ELN 实验记录 | eLabFTW、LabArchives、Benchling | 结构化模板、不可变时间戳、库存、签名 | 只取「结构化模板」一层；库存/签名/协作是实验室软件，明确不做 |
| 流程追踪 | Athenify、Thesis Flow、ResearchDock | 写作时长热力图、章节进度、导师协作跟踪 | 热力图与行动项落实是被验证的需求形态 |
| 用户抱怨（Reddit r/PhD 等） | — | 「PDF、笔记、引文、项目流程不在一个工具里」最普遍；导师会议产出落实是真实痛点；小绿鲸「笔记数据不可导出」是反面教材 | 印证方向二、三；重申数据导出与所有权 |

### 2.2 三个结论

1. **文献是最大缺口，但切入点是「线索」而非「阅读」**：研究生需要的是「我知道这篇文献读没读、读过之后变成了什么（灵感/任务/论文引用）、引用格式一键复制」；PDF 批注体验交给 Zotero。
2. **统计是零成本高回报**：投稿周期、项目投入、年度回顾全部可从现有 JSON 聚合，LetPub 仅靠「审稿周期数据」就撑起中文刚需产品。
3. **会议纪要与写作度量强化周报闭环**：周报自动生成已是格致优势，把会议行动项和写作投入作为新数据源接入，优势进一步加深。

## 3. 目标与非目标

### 3.1 目标（编号承接 PRD.md 的 G1–G5）

| # | 目标 | 说明 |
| --- | --- | --- |
| G6 | 洞察从数据中来 | 投稿周期、项目投入、活动热力图、年度回顾一页可见，全部由本地数据聚合，零手工统计 |
| G7 | 文献状态进入主链 | 文献条目可导入、可标状态、可复制引用，并能以最低摩擦生成灵感（读 → 记 → 转） |
| G8 | 导师会议产出零流失 | 会议纪要中的行动项一键转任务，任务完成自动回写勾选状态 |
| G9 | 写作投入可量化 | 手动计时 + 手录字数，热力图与周报自动呈现，无强制工作法 |
| G10 | 工程零膨胀 | 三个版本合计零新增 npm 依赖，安装包体积增幅 ≤ 2MB |

### 3.2 非目标（防臃肿红线，长期有效）

- **不做 PDF 阅读器与内置批注**——文献原文阅读交给 Zotero/系统阅读器，格致最多记录本地 PDF 路径并调用系统打开；
- **不做在线元数据抓取/文献推荐/引文图谱**——需要服务端与学术图谱数据，与零服务器架构冲突；
- **不做多人协作与云端同步**——维持本地明文 JSON + 网盘同步的既有架构；
- **不做完整 ELN**（库存、样本、电子签名、审计见证）；
- **不做番茄钟等强制工作法、不做字数目标与提醒**——计时器是「记录工具」不是「督促工具」；
- **不做应用内 AI 功能**——维持 PRD.md 决策，继续留在 Backlog 观察。

### 3.3 对既有决策的修订声明

本 PRD 不修改已冻结的 PRD.md，以下修订自本文档定稿起生效：

| 原决策 | 修订内容 |
| --- | --- |
| PRD.md 决策 D1「文献阅读管理排除」 | **部分修订**：仍排除 PDF 阅读、批注、元数据抓取；但以「文献线索层」形态（见 5.3）纳入 V2.5——导入 Zotero 导出数据、管理阅读状态、复制引用格式 |
| PRD.md 第 15 节范围外「数据看板/统计分析、番茄钟等专注工具」 | **部分修订**：统计分析纳入 V2.4（回顾页）；写作计时为手动起止记录（非番茄钟循环），番茄钟仍排除 |

## 4. 版本规划总览

| 版本 | 主题 | 内容 | 新增数据文件 | 依赖 |
| --- | --- | --- | --- | --- |
| **V2.4** | 深度回顾 | 回顾统计页（5.1）+ 进展日志模板（5.2） | 无（vocab.json 扩展 logTemplates 字段） | 无 |
| **V2.5** | 文献线索 | 文献条目管理、双格式导入、引用复制、主链串联（5.3） | `references.json` | 无 |
| **V2.6** | 会话与度量 | 导师会议纪要（5.4）+ 写作度量（5.5）+ 周报聚合扩展（5.6） | `meetings.json`、`sessions.json` | 5.6 依赖 5.4/5.5 的数据 |

每个版本独立可发布、可验收；三版共同遵循：**新增数据文件在旧数据目录中缺失时自动初始化为空集合，零迁移脚本；旧版本应用忽略不认识的文件，版本回退不破坏数据。**

## 5. 功能需求

> 优先级定义：P0 = 当版必须；P1 = 当版应有（可降级到下版）。

### 5.1 回顾与统计（Insights）【V2.4】

侧边栏「成果与汇报」组新增「回顾」页（置于「汇报中心」之后）。全部数据来自现有集合的只读聚合，本页**不产生任何新数据**。

#### 5.1.1 概览统计卡【P0】

**需求：**
1. 页首 4 张统计卡：本年完成任务数、进行中项目数、累计成果数、灵感转化率（状态为「已转换」的灵感占全部灵感比例）；
2. 统计口径固定为本年（1 月 1 日至今）+ 累计两种，卡片角落以浅色小字标注口径。

#### 5.1.2 活动热力图【P0】

**需求：**
1. GitHub 风格近 26 周（半年）热力图，按日计数，颜色深浅分 4 档（无活动 / 少 / 中 / 多）；
2. V2.4 计数口径：当日完成任务数 + 当日进展日志条数；V2.6 起（见 5.5）热力图增加口径切换器：「全部 / 仅写作」（写作口径 = 当日写作 session 计数）；
3. 悬浮任意格子显示日期与明细（完成任务 X 条、日志 Y 条〔、写作 Z 次〕）；
4. 空数据周正常渲染浅色底，不塌陷。

#### 5.1.3 投稿周期统计【P0】

**需求：**
1. 表格列出全部已有「投稿日期 + 结果通知日期」的论文：论文标题、目标 venue、投稿日期、出结果耗时（天数）；
2. 在投论文单独一节：列出状态为 已投稿/审稿中/大修/小修 的论文及「已等待天数」（投稿日至今），超过该 venue 历史均值的天数标黄；
3. 按 venue 分组计算平均审稿周期（≥2 篇才显示均值）与全部论文的总平均；
4. 无日期论文不参与统计，页脚注明排除数量。

#### 5.1.4 项目投入分布【P1】

**需求：**
1. 近 6 个月，每月按项目统计完成任务数，横向堆叠条形图（项目用既有颜色标识，「杂项任务」用灰色）；
2. 悬浮显示项目名与具体数量。

#### 5.1.5 年度回顾【P0】

**需求：**
1. 年份选择器（默认今年）；展示该年：完成任务数、进展日志条数、节点完成数、灵感新增数与转化数、成果时间线（复用成果台账的时间线组件）；
2. 「复制年度总结」按钮：将关键数字与成果清单复制为纯文本（格式同成果台账的复制功能），用于年终总结与个人简历。

**验收标准（5.1 全节）：**
- 统计数字与手工按 JSON 核对一致（抽样：投稿周期 ≥3 篇、热力图任选 3 天、年度回顾全部数字）；
- 5000 任务 / 3000 日志量级下，回顾页冷渲染 ≤ 1 秒，切换年份 ≤ 0.5 秒；
- 全部图表无横向滚动，880px 窄窗口单列堆叠（遵循 V3 设计规范）；
- 热力图颜色档位在深浅两种模式下均可分辨。

### 5.2 进展日志模板【V2.4】

**需求：**
1. 词汇库（vocab.json）新增 `logTemplates` 集合：`{ id, name, builtin, content }`，content 为 Markdown 骨架；
2. 内置模板 3 个（可改内容、不可删除）：**实验记录**（目的 / 方法与参数 / 结果 / 下一步）、**文献阅读**（文献信息 / 核心要点 / 对我的启发 / 待办）、**调试排错**（现象 / 假设 / 验证过程 / 结论）；
3. 用户可新建、编辑、删除自定义模板；内置模板可重命名与修改内容；
4. 项目详情页「进展日志」Tab：新建记录时，日期行旁新增「模板」下拉（默认「空白」）；选中模板即把骨架预填入内容区（预填后不锁定，可自由修改）；
5. 词汇库管理弹窗（VocabManagerModal）新增「日志模板」页签，与标签库、节点类型、成果类型并列管理。

**验收标准：**
- 从选择模板到骨架出现在编辑器 ≤ 1 秒，骨架内容可立即编辑；
- 删除自定义模板不影响已使用该模板写过的历史日志（模板只是录入时的一次性预填，日志内容独立存储）；
- 内置模板被「删除」时按钮置灰并提示不可删除。

### 5.3 文献线索层（References）【V2.5】

侧边栏「科研推进」组新增「文献」页（置于「时间节点」与「灵感」之间——对应「读文献 → 记灵感」的自然顺序）。核心定位：**管理「文献与我科研进度的关系」，不管理文献本身**。

#### 5.3.1 文献条目与列表【P0】

**需求：**
1. 文献字段：citekey（BibTeX 键）、条目类型（article / inproceedings / book / phdthesis / mastersthesis / techreport / misc）、标题、作者列表、年份、venue（期刊/会议名）、卷、期、页码、DOI、URL、标签、阅读状态、关联项目（可选）、笔记（Markdown，可空）、本地 PDF 路径（可选）、创建/更新时间；
2. 阅读状态为固定四态：**待读 / 在读 / 已读 / 略读**（不进词汇库，不可自定义——状态语义全局统一才有筛选价值）；
3. 列表页：表格视图（标题 / 作者 / 年份 / venue / 状态 / 标签），行内直接切换阅读状态；支持按状态、标签、项目、年份、全文（标题+作者+笔记）筛选与搜索；**分页加载，每页 100 条**（5000 条量级下普通渲染即可达标，不做虚拟滚动，见决策 D23）；
4. 排序默认按创建时间倒序（最新导入在前），可切换按年份、标题；
5. 条目详情浮层：完整字段 + 笔记编辑（Markdown）+ 关联项目 + PDF 路径；
6. 「打开 PDF」：调用系统默认阅读器打开本地路径（`shell.openPath`）；路径失效时置灰提示，操作同工具箱的失效交互；
7. 标签复用现有标签库（录入联想、级联重命名）。

#### 5.3.2 导入（BibTeX / CSL JSON）【P0】

**需求：**
1. 文献页「导入」按钮：选择 `.bib`（BibTeX，Zotero + Better BibTeX 导出）或 `.json`（CSL JSON，Zotero 原生导出）文件；
2. **导入预览**：解析后显示「将新增 X 条 / 更新 Y 条 / 跳过 Z 条（重复）/ 解析失败 N 条」，失败条目列出原因（如不支持 crossref）；用户勾选「重复条目用导入数据填充本地空缺字段」后 Y 才计入，默认不勾选；
3. **去重判定顺序**：DOI 相同 → citekey 相同 → 标题归一化（去空格、统一大小写）+ 年份相同；三者任一命中即视为重复；
4. 确认后执行导入，完成提示新增/更新/跳过/失败数；
5. 帮助文案（导入对话框底部小字）：说明 Zotero 两种导出路径（Better BibTeX 导出 .bib；文件 → 导出 CSL JSON）；
6. 解析在**主进程**执行（大文件不阻塞渲染进程），结果经 IPC 返回；
7. 不提供导出到 Zotero 的反向同步——导入是单向快照，重复导入靠去重合并（见决策 D11）。

#### 5.3.3 引用格式复制【P0】

**需求：**
1. 列表行与详情浮层提供「复制引用」操作，三种格式：
   - **GB/T 7714—2015（顺序编码制）**：按条目类型输出文献类型标识（期刊 [J]、会议论文集 [C]、图书 [M]、学位论文 [D]、报告 [R]、电子资源 [EB/OL]）；作者 ≤3 位全列，>3 位列前 3 位后加「等」（中文文献）或「et al.」（英文文献，按标题是否含 CJK 字符判定）；英文作者按「姓全大写 + 名缩写」（如 EINSTEIN A）；
   - **BibTeX**：由存储字段重新序列化（`@article{citekey, title = {...}, ...}`），citekey 原样保留；
   - **APA 第 7 版（简化）**：`作者. (年). 题名. *Venue*, 卷(期), 页码. doi/URL`；作者简化规则同上；
2. 复制后行内 toast 提示已复制及格式名；
3. 支持在列表勾选多条后批量复制（选中项按顺序编码制生成带 [1][2] 编号的 GB/T 7714 引用块，其余两种格式以空行分隔）。

#### 5.3.4 与主链的串联【P0】

**需求：**
1. **文献 → 灵感**：文献条目详情提供「记一条灵感」按钮，唤起全局速记框并预填 `[文献] {标题}：`前缀；不在 Idea 上增加文献外键（见决策 D11）；
2. **文献 → 项目**：条目可关联项目，项目维度回答「这个方向我读过哪些」；
3. **文献 → 论文（引用清单）**：条目可标记「已引用于」某篇本地 Paper（多选）；Paper 编辑弹窗新增「引用文献」只读摘要区，点击文献名跳转文献页——这是写作时的 quick check：投稿前核对相关工作引用是否覆盖。

**验收标准（5.3 全节）：**
- 导入产品所有者的真实 Zotero 导出文件（≥100 条）解析成功率 ≥ 98%，失败条目全部有明确原因；
- 2000 条文献量级下：导入解析 ≤ 10 秒且 UI 不冻结，列表筛选/搜索 ≤ 0.5 秒，滚动无卡顿；
- 三种引用格式对期刊/会议/学位论文/图书四类条目各抽查 1 条，与 6.1 节格式定义逐字符一致；
- 从文献详情到速记框可输入 ≤ 1 秒，保存的灵感含文献标题前缀；
- PDF 路径存在时一键打开系统阅读器；路径失效时置灰且提供重新指定。

### 5.4 导师会议纪要（Meetings）【V2.6】

侧边栏「科研推进」组新增「会议纪要」页（置于「灵感」之后）。定位：**带行动项的结构化会谈记录**——与进展日志的分工：日志 = 项目过程流水（按项目归属），会议 = 人与人的会谈产出（可跨项目，核心价值在行动项落实）。

**需求：**
1. 会议字段：日期、标题（如「与 X 老师周会」）、参与人（自由文本）、关联项目（可选，可空表示跨项目）、讨论纪要（Markdown）、行动项列表、创建/更新时间；
2. 行动项：`{ 文本, 是否完成, 关联任务 id }`；可逐条添加、编辑、删除、手动勾选完成；
3. **行动项一键转任务**：预填任务标题 = 行动项文本、项目 = 会议关联项目（若有）；转成功后行动项显示任务链接图标，点击跳转任务；
4. **完成态单向同步**：关联任务完成后，行动项自动置为完成（任务 → 行动项单向；行动项手动勾选不影响任务）；
5. 会议列表页：按日期倒序时间线，卡片显示标题、日期、参与人、项目色条、行动项完成进度（如 2/5）；支持按项目筛选、按标题/内容全文搜索；
6. 行动项未完成的会议在列表卡片上有醒目标记（防止「会上说好会后忘」）；
7. 项目详情页**不新增**「会议」Tab（避免与进展日志 Tab 混淆，见决策 D20）。

**验收标准：**
- 新建会议必填仅标题 + 日期，10 秒内可完成；
- 行动项转任务后：任务出现在任务列表、行动项出现链接、任务完成后行动项自动打勾（端到端验证）；
- 未完成行动项的会议在列表上一眼可辨；
- 删除会议前二次确认（行动项关联的任务不被级联删除，仅解除回链）。

### 5.5 写作度量（Writing Sessions）【V2.6】

**需求：**
1. 写作 Session 字段：开始时间、结束时间（ISO，时长派生计算不落盘）、关联项目（可选）、关联论文（可选）、章节（自由文本如「3.2 实验设计」，可空）、本次字数（手录整数，可空）、备注、创建时间；
2. **计时器入口**：侧边栏底部「写作计时」按钮；点击开始 → 按钮变为计时显示（mm:ss，每秒刷新）；再次点击结束 → 弹出记录小窗（显示时长，录字数/关联项目/论文/章节/备注）保存；
3. 全局同一时刻最多一个进行中的 session；开始时若上次异常残留未结束的 session（应用崩溃等），先提示补记（以开始时间 +4 小时为默认结束时间，可修改）；
4. **忘记停止防护**：结束弹窗中，时长 > 4 小时时黄字提醒「时长较长，请确认是否忘记停止」，起止时间均可微调；
5. **应用退出收尾**：退出（含托盘退出）时有计时中的 session，以退出时刻自动结束并保存（备注自动追加「应用退出自动结束」）；
6. 记录列表在回顾页呈现：热力图增加「仅写作」口径（见 5.1.2）；按周/月统计总时长与总字数；
7. 不做字数目标、番茄循环、专注锁屏、写作排行榜（非目标红线）。

**验收标准：**
- 从点击「写作计时」到计时开始 ≤ 0.5 秒；计时中切换页面、最小化到托盘计时持续；
- 忘记停止防护：构造 >4h session 出现提醒，且起止时间可修改后正确保存；
- 退出自动收尾后重启应用，该 session 出现在记录中且备注含自动结束标记；
- 周统计与原始 session 逐条核对一致（时长求和、字数求和）。

### 5.6 周报聚合扩展【V2.6】

**需求：**
1. 周报/月报生成新增两个可选占位符：`{{MEETINGS}}`（周期内会议列表 + 各会议行动项及完成状态）、`{{WRITING}}`（周期内写作总时长、总字数、按论文分组明细）；
2. 占位符为**可选**：用户自定义模板中不含则该块不出现，向后兼容；
3. 默认模板更新：在「一、本期工作」末尾追加 `{{WRITING}}` 块；`{{MEETINGS}}` 以子块并入本期工作（旧用户自定义模板不变）；
4. 导出 Word（docx）同步支持新块。

**验收标准：**
- 生成周期内含 2 场会议、5 个 session 的周报，新块内容完整且数字与源数据一致；
- 使用不含新占位符的旧自定义模板生成周报，内容与升级前完全一致（回归验证）。

## 6. 数据模型扩展

> 与 PRD.md 第 8 节风格一致：此处定义实体与关系作为共识基础，详细字段由技术方案落实。

| 实体 | 关键字段 | 关系 | 落入文件 |
| --- | --- | --- | --- |
| Reference 文献 | citekey, entry_type, title, authors[], year, venue, volume/issue/pages, doi, url, tags[], status(4态), project_id?, note(md), pdf_path? | N—1 Project（可空）；被 Paper 引用（多选标记）；可经速记预填关联 Idea（无外键） | `references.json` |
| Meeting 会议 | date, title, attendees, project_id?, content(md), action_items[]{text, done, task_id?} | N—1 Project（可空）；行动项可关联 Task | `meetings.json` |
| WritingSession 写作 | started_at, ended_at, project_id?, paper_id?, section, words?, note | N—1 Project / Paper（均可空） | `sessions.json` |
| LogTemplate 日志模板（V2.4） | name, builtin, content(md 骨架) | 被 ProgressLog 录入时一次性预填引用（无外键） | `vocab.json` 扩展字段 |
| Paper 论文（扩展） | 新增引用文献标记 cited_reference_ids[]（id 数组，仅存储与跳转用） | N—N Reference | `papers.json` 字段扩展 |

**类型系统变更点**（开发依据）：
- `CollectionName` 增加 `'references' | 'meetings' | 'sessions'`；`COLLECTION_FILES` 增加三个文件名映射；`AllCollections` 相应扩展；
- `VocabFileData` 增加 `logTemplates: LogTemplate[]`（旧数据目录无此字段时兜底为内置三模板）；
- `Paper` 增加 `cited_reference_ids: string[]`（旧数据缺省 `[]`）；
- 三个新集合文件缺失时初始化为空数组；**不做任何迁移脚本，不修改既有字段**。

## 7. 非功能需求

### 7.1 性能

1. 文献：5000 条量级下导入解析 ≤ 10 秒（主进程，UI 不冻结）；列表筛选/搜索 ≤ 0.5 秒；引用复制即时；
2. 回顾页冷渲染 ≤ 1 秒；写作计时器计时刷新不引起其他组件重渲染；
3. 其余页面在数据量增长后性能不劣化于现状（新数据文件独立，不影响既有集合读写）。

### 7.2 数据安全与同步（延续既有机制）

1. 三个新 JSON 文件全部纳入既有机制：原子写入、防抖落盘、每日/退出自动备份（backups/ 保留 30 份）、网盘同步检测自动重载；
2. `references.json` 预估体量：5000 条约 3–5MB，为全部集合中最大，仍在单文件 JSON 可接受范围；若未来网盘流量成为问题，再评估分片（超出本 PRD 范围）；
3. 版本回退安全：旧版本应用忽略不认识的新文件；新版本应用对旧数据目录零迁移兼容。

### 7.3 易用性与界面

1. 新页面全部遵循 [V3 设计规范](design/V3-DESIGN-SPEC.md)：7 档字号、8pt 网格、主题感知圆角 token、Chips + 锚定区筛选条结构；
2. 全中文界面；列表页核心路径支持键盘操作；
3. 布局自适应 880px–全屏：文献表格窄窗降级为卡片堆叠或横向收纳列；热力图/条形图无横向滚动。

## 8. 技术方案考虑

> 推荐方案，允许在不违背第 7 节的前提下调整。

### 8.1 零新增依赖约束（决策 D14）

三个版本**合计零新增 npm 依赖**：

1. **BibTeX 解析自研**（主进程）：覆盖常见子集——`@article / @inproceedings(@conference) / @book / @phdthesis / @mastersthesis / @techreport / @misc(@online,@electronic)`、`@string` 宏展开、`#` 字符串拼接、`{}` 与 `""` 两种值包裹、month 宏、大小写不敏感的条目名；不支持 crossref（该条目按可解析字段导入并在报告中提示）；解析失败单条跳过并记录原因，**绝不静默丢弃**；
2. **CSL JSON**：标准 JSON 直接映射（author 数组拼接、issued 年份提取）；
3. **引用格式化**：三个纯函数（GB/T 7714 / BibTeX / APA），放 `renderer/src/lib/citation.ts`，CJK 判定用正则 `[\u4e00-\u9fff]`；
4. **图表自绘**：热力图为 CSS Grid 色块，堆叠条形图为 Flex 宽度百分比，无图表库。

### 8.2 导入与去重数据流

渲染层选文件 → IPC 至主进程读取+解析 → 返回结构化条目数组 → 渲染层与现有 references 比对去重（DOI → citekey → 标题+年份）生成预览 → 用户确认后经现有 store 写入路径合并落盘（复用防抖与原子写）。

### 8.3 计时器实现

写作计时状态（进行中 session 的 started_at）放 Zustand 独立小 store（不落盘，仅内存），每秒刷新仅作用于计时器组件；异常残留检测在应用启动时执行（存在 started_at 无 ended_at 的记录即触发补记流程）。跨设备同步场景下，网盘同步进行中的 session 文件后另一台设备同样触发补记提示——与「一次只用一台设备」使用约定一致。

## 9. 成功指标

上线后观察 8 周：

| 类别 | 指标 | 目标 |
| --- | --- | --- |
| 文献 | 真实 Zotero 库导入（≥100 条）解析成功率 | ≥ 98% |
| 文献 | 阅读状态更新频次 / 引用复制使用 | 每 2 周 ≥ 10 次 / 累计 ≥ 10 次 |
| 会议 | 每次导师会议 24 小时内录入；行动项转任务率 | 100% |
| 写作 | 平均每周写作 session 数 | ≥ 3 |
| 回顾 | 投稿周期统计被用于 ≥ 1 次真实决策（改投/催稿/汇报沟通） | 达成 |
| 周报 | 生成草稿含会议与写作块且无需手工补数 | 默认达成 |
| 工程 | 安装包体积增幅 / 新增 npm 依赖 | ≤ 2MB / 0 |
| 可靠 | 数据丢失事件（含导入失败可恢复） | 0 起 |

## 10. 风险与对策

| 风险 | 概率/影响 | 对策 |
| --- | --- | --- |
| BibTeX 方言兼容（Zotero / Better BibTeX / EndNote / Google Scholar 导出差异） | 中 / 中 | 解析失败单条报告原因不静默丢弃；首个验收样本为产品所有者真实导出文件；解析器覆盖面按 8.1 明确边界，不做大而全 |
| 文献功能需求蔓延成文献管理器（PDF 批注、在线抓取、影响因子） | 高 / 高 | 3.2 红线 + 决策 D10；新想法一律进 Backlog 不进当期版本 |
| 忘记停止计时导致数据失真 | 高 / 低 | >4h 确认提醒 + 起止时间可微调 + 退出自动收尾 |
| 会议纪要与进展日志功能混淆 | 中 / 中 | 定位文档化（5.4 开头分工声明）；项目详情不加会议 Tab；术语表区分 |
| references.json 全量写带来网盘同步流量 | 低 / 中 | 防抖批量写已缓解；5000 条约 3–5MB 可接受；成为实际问题时再评估分片 |
| 三版本周期拉长烂尾 | 中 / 高 | 每版独立可发布可验收；V2.4 零新实体最小先行，2 周内可交付建立信心 |

## 11. 决策记录（Decision Log，编号承接 PRD.md D1–D8）

以下事项已于 2026-08-22 评审议定：

| # | 事项 | 决策 | 落实位置 |
| --- | --- | --- | --- |
| D9 | 新阶段 PRD 文档组织 | **独立成文**（本文件）。PRD.md 冻结为 V1–V2.3 历史定稿，新文档独立演进；README 文档区挂链接 | 全局 |
| D10 | 修订原 D1 与原第 15 节范围外 | 文献以**线索层**形态纳入（仍排除 PDF 阅读/批注/抓取）；统计分析纳入 V2.4；番茄钟仍排除（手动计时器 ≠ 番茄钟） | 3.2、3.3、5.3 |
| D11 | 文献导入与引用格式范围 | 导入**双格式**（BibTeX .bib + CSL JSON .json）；引用复制**三种**（GB/T 7714 / BibTeX / APA 7 简化）；去重键 DOI → citekey → 标题+年份；导入为单向快照，不做反向同步 | 5.3.2、5.3.3 |
| D12 | 导师会议纪要实体设计 | **独立实体** meetings.json，行动项内嵌（text/done/task_id）；完成态任务→行动项单向同步；项目详情不加会议 Tab | 5.4、6 |
| D13 | 写作度量产品形态 | **手动计时器 + 手录字数**；不做番茄循环、字数目标、自动字数统计（Overleaf 集成不做）；>4h 防呆 + 退出自动收尾 | 5.5 |
| D14 | 依赖约束 | 三个版本**零新增 npm 依赖**：BibTeX 解析自研、图表 CSS 自绘、格式化纯函数 | 8、9 |
| D15 | 灵感与文献的关联方式 | **速记预填文本**（`[文献] 标题：` 前缀），不在 Idea 增加文献外键——避免 Idea 编辑弹窗增加选择器与数据迁移，溯源靠前缀文本与时间 | 5.3.4 |
| D16 | 导入冲突默认策略 | **默认跳过重复**；仅当用户勾选「用导入数据填充本地空缺字段」时才执行更新，首次导入体验更安全 | 5.3.2 |
| D17 | 阅读状态枚举 | **固定四态**（待读 / 在读 / 已读 / 略读），不进词汇库——状态语义全局统一才有筛选价值 | 5.3.1 |
| D18 | 写作计时器入口 | **侧边栏底部常驻按钮**——常驻才符合「随手开始写作」的使用方式 | 5.5 |
| D19 | Paper 与文献的引用标记 | **做**：Paper 增加 `cited_reference_ids` 多选标记 + 编辑弹窗只读摘要区，支撑投稿前核对相关工作引用 | 5.3.4、6 |
| D20 | 会议在项目维度的可见性 | V2.6 **仅会议页按项目筛选**，项目详情不加会议 Tab；项目详情「相关会议」小节视使用反馈进 Backlog | 5.4 |
| D21 | 默认周报模板更新幅度 | 在「一、本期工作」内**追加**写作块与会议子块，保持三期结构，改动最小 | 5.6 |
| D22 | 回顾页热力图时间窗 | **固定近 26 周**（半年）；13/52 周切换视使用反馈再议 | 5.1.2 |
| D23 | 文献列表大数据量方案 | **普通渲染 + 分页（每页 100 条）**；5000 条量级已达标，不引入虚拟滚动复杂度 | 5.3.1、7.1 |

## 12. 待确认事项（已于 2026-08-22 全部确认）

初稿遗留的 8 项微观决策已由产品所有者逐项确认，**全部采纳推荐方案**，正式记入决策记录 D16–D23：

| # | 事项 | 确认结果（= 决策） |
| --- | --- | --- |
| 1 | 导入冲突默认策略 | 默认跳过重复，勾选后才填充空缺字段（D16） |
| 2 | 阅读状态枚举 | 固定四态，不进词汇库（D17） |
| 3 | 写作计时器入口 | 侧边栏底部常驻按钮（D18） |
| 4 | Paper 与文献的引用标记 | 做，`cited_reference_ids` 多选标记 + 摘要区（D19） |
| 5 | 会议在项目维度的可见性 | 仅会议页按项目筛选，项目详情不加 Tab（D20） |
| 6 | 默认周报模板更新幅度 | 本期工作内追加写作块与会议子块（D21） |
| 7 | 回顾页热力图时间窗 | 固定近 26 周（D22） |
| 8 | 文献列表大数据量方案 | 普通渲染 + 分页每页 100 条，不做虚拟滚动（D23） |

**本 PRD 当前无未决问题，可按第 4 节版本规划启动 V2.4 开发。**

## 附录 A：术语表（新增部分，接续 PRD.md 附录 A）

| 术语 | 定义 |
| --- | --- |
| 文献线索（Reference） | 从 Zotero 导入的文献条目及其阅读状态、笔记与关联关系；管理「文献与科研进度的关系」，不管理文献原文 |
| citekey | BibTeX 条目的引用键（如 `einstein1905`），去重与 BibTeX 复制的依据 |
| 阅读状态 | 文献的四态标记：待读 / 在读 / 已读 / 略读 |
| 行动项（Action Item） | 会议纪要中拆出的待落实事项，可转任务并单向回写完成态 |
| 写作 Session | 一次手动起止的写作记录：时长、字数（手录）、关联论文与章节 |
| 日志模板（Log Template） | 进展日志录入时的 Markdown 骨架预填源，内置实验记录/文献阅读/调试排错三种 |
| 回顾页（Insights） | 只读统计聚合页：统计卡、热力图、投稿周期、项目投入、年度回顾 |

## 附录 B：修订记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| V1.0 | 2026-08-22 | 初稿：基于竞品调研与四方向讨论形成；四个关键决策（独立文档/双格式导入+三引用/会议独立实体/手动计时器）经产品所有者确认；遗留第 12 节 8 项待确认事项 |
| V1.1 | 2026-08-22 | 评审定稿：第 12 节 8 项待确认事项全部按推荐方案确认，记入决策记录 D16–D23；5.3.1 补充分页方案（每页 100 条）；文档状态更新为「全部决策已确认，可启动开发」 |
