# 格致 · 科研工作台（Gezhi Research Workbench）产品需求文档

| 项目 | 内容 |
| --- | --- |
| 文档版本 | V1.2 |
| 发布日期 | 2026-08-21（V1.0：2026-08-20；V1.1：2026-08-21） |
| 状态 | 已定稿，待启动开发 |
| 产品名称 | 格致（Gezhi）· 科研工作台 |
| 产品形态 | Windows 桌面应用（单用户） |
| 开发方式 | AI 辅助开发，产品所有者提需求并验收 |
| 评审人 | 产品所有者（硕士研究生本人） |

---

## 1. 概述

格致（Gezhi）是一款为硕士研究生个人打造的**单机桌面应用**，用于集中管理科研生活中原本散落在各处的信息：项目与任务、关键时间节点（开题、投稿截止、中期检查等）、实验与项目进展、灵感想法、论文投稿状态、工作汇总以及常用工具入口。

产品的核心主张是：**一台电脑上的一个应用，装下我全部的科研管理工作。** 它不追求多人协作和云端能力，而是通过「本地文件存储 + 网盘同步目录」的方式，在保持零运维成本的前提下实现多台电脑间的数据同步。

产品分两期交付：
- **V1（核心闭环）**：项目、任务列表、日历、关键时间节点、今日概览、灵感速记、工具收藏、项目进展日志——形成「每天打开 → 知道今天做什么 → 随手记录」的完整闭环。
- **V2（增强）**：看板视图、论文写作与投稿跟踪、周报自动生成、成果台账、汇报导出——从「管事」扩展到「产出」。

**命名说明**：应用定名「格致」，取自「格物致知」，也是近代汉语对科学（Science）的旧译，寓意推究事物、求得真知——与个人科研工作台的定位契合；开发中使用的标识符为 `gezhi-workbench`。

## 2. 背景与问题陈述

### 2.1 现状

一名硕士研究生的科研管理信息目前散落在多个互不联通的地方：

- 任务和排期记在 Excel、备忘录或脑子里；
- 开题、投稿截止等关键日期靠微信收藏的截图和记忆；
- 实验进展记在各种草稿文件里，写周报时找不到；
- 灵感想法在走路、开会时冒出来，没有顺手的地方记录，事后遗忘；
- 常用工具（文献数据库、计算平台、VPN、数据集目录）散落在浏览器书签、桌面快捷方式和聊天记录里。

### 2.2 问题

1. **信息碎片化**：同一项目的任务、进展、节点分布在不同工具中，无法在一个视图里看到项目全貌。
2. **Deadline 依赖记忆**：关键节点没有统一的倒计时视图，存在错过投稿截止、临时赶材料的风险。
3. **周报成本高**：每周向导师汇报时需要靠回忆拼凑本周工作，耗时且容易遗漏。
4. **灵感流失**：想法产生时没有低摩擦的记录渠道。
5. **通用工具不适配**：Notion/Obsidian 等通用工具需要自行搭建且偏笔记向，项目管理类工具（如 Jira）又过重且为团队设计。

### 2.3 为什么不直接用现成工具

通用笔记工具缺少「项目—任务—节点—汇总」的结构化管理能力，需要大量手工搭建；在线项目管理工具为团队设计，单人使用过重，且数据不在自己手里。一个轻量、本地、为科研节奏（学期节点、投稿周期、导师汇报）定制的单机应用，是当前的最优解。

## 3. 目标与非目标

### 3.1 目标

| # | 目标 | 说明 |
| --- | --- | --- |
| G1 | 科研管理信息单点集中 | 项目、任务、节点、灵感、进展、工具全部在同一应用内管理 |
| G2 | Deadline 零遗漏 | 所有关键节点录入后，应用内可见倒计时与今日概览提醒 |
| G3 | 记录零摩擦 | 灵感速记从唤起到保存 ≤ 3 秒（全局快捷键） |
| G4 | 周报从回忆变成整理 | V2 周报草稿自动聚合本周完成任务与进展记录，人工只做润色 |
| G5 | 数据自主可控 | 数据为本地明文格式（JSON），存于网盘同步目录，零服务器成本，可随时备份迁移 |

### 3.2 非目标（明确不做）

- 多用户、多人协作、权限管理；
- 移动端 App；
- 内置文献管理与 PDF 阅读（文献管理交给 Zotero 等专业工具，本应用仅通过工具收藏提供入口）；
- 云端账号体系与应用内置云同步；
- 邮件、短信等应用外提醒渠道；
- 应用内 AI 功能（如自动摘要、智能问答）——列为未来可选方向，不进入 V1/V2 范围。

## 4. 用户画像与核心场景

### 4.1 用户画像

**产品所有者本人**：硕士研究生，同时推进 1–3 个研究方向/项目；主要在实验室与宿舍两台 Windows 电脑上工作；数据通过网盘在两台电脑间同步；有导师周汇报压力；自己不直接编写主要代码，依靠 AI 辅助开发并负责验收。

### 4.2 核心使用场景

**场景 A：早晨到实验室，打开工作台**
启动应用直接落在「今日概览」：今天到期的任务 2 项、距某会议论文截稿还有 12 天（黄色预警）、昨天记的一条灵感待整理。花 5 分钟明确今天的安排。

**场景 B：导师新布置一个方向**
新建项目「XX 方向预研」，把大目标拆成 6 个任务并设截止日；把「一个月后出初步实验结果」登记为项目级时间节点。

**场景 C：读论文时冒出想法**
不切换窗口，按 `Alt+N` 弹出速记框，输入「对比 XX 方法在低信噪比下的表现，可能可以作为消融实验」，回车保存，继续读论文。周末在灵感页统一整理，把有价值的转成任务。

**场景 D：周日晚上写周报（V2）**
点击「生成本周周报草稿」，应用自动聚合本周完成的任务、新增的进展日志、灵感摘要，生成 Markdown 草稿；润色后导出发给导师。

**场景 E：需要打开常用工具**
在「工具箱」点击「Zotero 文献库」打开网页、点击「数据集目录」打开本地文件夹、点击「学校 VPN」启动客户端，无需在书签和桌面图标里翻找。

## 5. 产品结构（信息架构）

侧边导航（V1）：

```
今日概览（默认启动页）
项目
  └─ 项目详情：概览 / 任务 / 进展日志 / 时间节点
日历
灵感
工具箱
```

V2 新增导航：`论文投稿`、`成果台账`、`汇报中心`；任务模块内增加看板视图切换。

## 6. 功能需求

优先级定义：P0 = V1 必须；P1 = V1 应有（可降级）；P2 = V2。

### 6.1 今日概览（Dashboard）【P0】

启动应用后的默认页面，一屏回答「今天做什么、最近有什么大事」。

**需求：**
1. 今日到期与逾期任务列表（逾期置顶并标红，可直接勾选完成）；
2. 未来 30 天内的关键时间节点倒计时列表，按临近程度排序，分级显示：≤7 天红色、≤30 天黄色；
3. 未处理灵感快捷入口（最近 3 条，可一键转为任务）；
4. 快速添加入口：新建任务、新建灵感、新建节点的按钮；
5. 显示数据目录同步状态（上次数据写入时间），便于确认网盘在工作。

**验收标准：**
- 打开应用到今日概览渲染完成 ≤ 2 秒；
- 勾选任务后列表无需刷新立即更新状态；
- 逾期任务一定出现在列表最顶部且视觉上与正常任务明显区分。

### 6.2 项目管理【P0】

项目是顶层组织单元，对应一个研究方向、一篇论文或导师布置的一个课题。

**需求：**
1. 项目字段：名称、描述、颜色标识、状态（进行中 / 已完成 / 已归档）、开始日期、预计结束日期；
2. 项目列表页：进行中项目卡片（含任务完成进度条、最近节点倒计时）；已完成/归档项目折叠展示；
3. 项目详情页包含四个 Tab：概览、任务、进展日志、时间节点（见 6.3–6.8）；
4. 归档项目不参与各视图默认统计，但可随时查看与恢复。

**验收标准：**
- 新建项目所需填写字段仅名称为必填，10 秒内可完成创建；
- 项目卡片准确显示「已完成任务数 / 总任务数」进度；
- 删除项目需二次确认，且系统先自动备份被删数据（见 9.3）。

### 6.3 任务管理【P0 列表视图 / P2 看板视图】

**需求（V1 列表视图）：**
1. 任务字段：标题、所属项目（可选，允许无项目的杂项任务）、状态（待办 / 进行中 / 已完成）、优先级（高 / 中 / 低）、截止日期（可选）、备注、标签；
2. 任务列表按项目分组展示，组内默认按「截止日期升序 + 优先级降序」排序；
3. 支持筛选：按项目、状态、标签、截止日期区间；
4. 快速操作：一行内勾选完成、行内编辑截止日期与优先级；
5. 逾期任务自动标记（基于系统日期与截止日期比较）；
6. 任务支持从灵感一键转换创建（预填标题，保留来源关联）；
7. 标签支持从**标签库**选择联想或直接输入新标签（V1.2，见 6.11 词汇库管理）。

**需求（V2 看板视图）：**
1. 三列看板：待办 / 进行中 / 已完成，卡片支持拖拽换状态；
2. 看板与列表视图数据完全同源，切换视图不丢筛选条件。

**验收标准：**
- 新建任务仅需输入标题，回车即保存，其余字段事后补充；
- 勾选完成后任务自动移入已完成分组，并记录完成时间（供周报聚合使用）；
- 拖拽改状态（V2）后刷新应用，状态保持正确。

### 6.4 日历视图【P0】

**需求：**
1. 月视图为默认视图，支持切换周视图；
2. 日历展示三类内容：任务截止日（按项目颜色标记）、时间节点（特殊图标区分类型）、纯日程；
3. 点击某天弹出该日明细浮层，可直接添加任务或节点；
4. 点击日历中的条目跳转到对应任务/节点详情。

**验收标准：**
- 同一天存在多条内容时不遮挡、不重叠（折叠为「+N 更多」）；
- 月视图横向宽度下无需横向滚动即可看完整月。

### 6.5 关键时间节点【P0】

节点是全局日历级的重要日期，分为：全局节点（如学位论文开题、中期检查）与项目级节点（如某论文投稿截止、某会议 Rebuttal 截止）。

**需求：**
1. 节点字段：标题、日期、类型（开题 / 投稿截止 / 会议 / 中期检查 / 答辩 / 其他 + **用户自定义类型**，V1.2）、所属项目（可选）、提前提醒天数（默认 7/3/1 天，可自定义）、备注、状态（未到期 / 已完成 / 已过期）；
2. 节点总览列表页：按日期排序展示全部未完成节点与倒计时，可按类型、项目筛选；
3. 提醒方式为**应用内提醒**：达到提醒窗口的节点出现在今日概览的倒计时列表（红色/黄色分级）；应用不做系统弹窗、不做外部通知（非目标）；
4. 节点可标记「已完成」（如已投稿），完成后从倒计时列表消失、进入历史记录；
5. 类型支持用户自定义（V1.2）：新建/编辑节点时可在下拉中直接新建类型；内置类型可改名不可删除，自定义类型被引用时不可删除（见 6.11）。

**验收标准：**
- 新建节点必填仅标题 + 日期；
- 进入提醒窗口（如提前 7 天）后，节点一定出现在今日概览且颜色分级正确；
- 同一日期支持多个节点，互不覆盖。

### 6.6 灵感速记【P0】

**需求：**
1. **全局快捷键捕获**：应用运行中（含最小化到托盘），按 `Alt+N`（可配置）在任何界面弹出轻量输入框，输入后 `Ctrl+Enter` 保存，`Esc` 取消，全程不离开当前工作环境；
2. **应用内管理页**：全部灵感按时间倒序展示，支持标签、全文搜索、按项目/标签筛选；
3. 灵感字段：内容（支持多行文本）、标签、关联项目（可选）、创建时间、状态（新想法 / 已整理 / 已转换）；
4. 灵感可一键「转为任务」：创建任务并回填标题，灵感状态变为「已转换」，保留双向关联；
5. 快捷键冲突处理：快捷键可在设置中修改；注册失败（被其他软件占用）时应用内提示。

**验收标准：**
- 从按下快捷键到输入框可输入 ≤ 0.5 秒；
- 快速连续录入多条灵感（保存后不关闭输入框，可继续录下一条）；
- 保存的灵感在重启应用后完整存在（含换行等格式）。

### 6.7 工具收藏（工具箱）【P0】

**需求：**
1. 收藏项类型：网址、本地文件、本地文件夹、本地程序；
2. 收藏项字段：名称、类型、路径/URL、所属分组、备注；网址自动抓取 favicon 作为图标，本地项用类型默认图标；
3. 分组管理：自定义分组（如「文献」「计算」「学校服务」「数据」），分组可排序，支持拖拽调整收藏项分组；
4. 点击行为：网址调用系统默认浏览器打开；本地文件/文件夹/程序调用系统默认关联方式打开；
5. 失效检测：本地路径不存在时图标置灰并提示「路径不存在」，提供一键重新指定路径；
6. 首次启动预置少量可编辑的示例收藏（如 Zotero 官网、知网、Google Scholar、arXiv），解决空状态问题，并作为文献管理入口（见决策 D1），用户可修改或删除。

**验收标准：**
- 新增网址收藏仅需粘贴 URL，名称自动从网页标题获取（获取失败时用域名）；
- 点击本地程序能正常启动该程序；
- 被移动过位置的本地收藏项有明确的灰色失效态，不会点击无反应。

### 6.8 项目进展日志【P0 轻量版】

以时间线形式记录每个项目的进展（实验、讨论、阶段结论），是 V2 周报自动聚合的数据来源。

**需求：**
1. 每个项目详情页「进展日志」Tab：按日期倒序的时间线，每条记录包含日期、内容（支持 Markdown：列表、代码块、加粗）；
2. 快速追加：页面顶部常驻「记一笔」输入区，默认带今天日期；
3. 支持补记历史日期的记录；
4. 每条记录可编辑与删除；
5. （V2 增强：附件/图片、代码仓库链接的结构化字段、实验参数模板。）

**验收标准：**
- 从进入项目详情到完成一条记录保存 ≤ 15 秒；
- Markdown 的代码块与列表在展示态正确渲染。

### 6.9 论文写作与投稿跟踪【P2 / V2】

**需求：**
1. 论文条目字段：标题、关联项目、目标期刊/会议、类型（期刊/会议）、状态、当前轮次、重要日期（初稿/投稿/结果/_camera-ready）、代码仓库链接、协作人备注；
2. 状态流转：构思 → 写作中 → 已投稿 → 审稿中 → 大修 → 小修 → 录用 / 拒稿（可回退，拒稿后可改投并重置状态）；
3. 写作章节清单：每篇论文可维护章节级任务（如「引言」「实验」），与任务系统联动（章节任务可同步出现在任务列表，打勾双向同步）；
4. 状态变更与投稿截止等关键日期自动生成对应的时间节点（如「XX 会议投稿截止」）；
5. 论文列表视图：按状态分组的表格，一屏看清所有在投/在写的论文。

**验收标准：**
- 状态变更从列表页两步内完成（点开 → 选择新状态）；
- 录用论文自动进入成果台账（见 6.10）草稿项。

### 6.10 工作汇总（V2）

#### 6.10.1 周报/月报草稿自动生成【P2】

**需求：**
1. 一键选择周期（本周/上周/本月），应用聚合该周期内的：已完成任务（按项目分组）、进展日志条目、新增灵感摘要、发生的节点事件；
2. 按内置模板生成 Markdown 草稿，模板字段：本周工作（按项目分节）、下周计划（默认带入下周到期任务）、问题与思考（空，人工填）；
3. 草稿可人工编辑，编辑后保存为正式版本；
4. 历史周报归档可查，支持对比查看。

**验收标准：**
- 生成的草稿包含该周期内全部已完成任务，无遗漏（抽样核对）；
- 下周计划默认带入下周 7 天内到期且未完成的任务；
- 模板可在设置中自定义（占位符方式）。

#### 6.10.2 成果台账【P2】

**需求：**
1. 成果类型：论文、专利、获奖、项目、其他；字段：类型、名称、日期、级别（如 CCF-A / SCI 一区 / 校级）、关联项目、详情、证明材料路径；
2. 时间线视图 + 按类型筛选；列表可复制为纯文本（用于填简历/年终总结）。

#### 6.10.3 汇报导出【P2】

**需求：**
1. 周报/月报导出为 Markdown（`.md`）与 Word（`.docx`）两种格式；
2. 导出文件命名规范：`周报_2026_第34周_YYYYMMDD.docx`；
3. 支持导出指定时间段的「工作总结」（任务统计 + 成果列表 + 进展摘要）。

**验收标准：**
- 导出的 `.docx` 在 Word/WPS 中打开无格式错乱（标题层级、列表正确）。

### 6.11 词汇库管理（标签与节点类型）【P0，V1.2 新增】

统一管理用户可自选的枚举值，避免标签越用越散、节点类型不够用。

**需求：**
1. 管理入口在「设置 → 标签与节点类型」；
2. **标签库**：添加、重命名、移除；任务/灵感录入时从标签库联想选择，也可直接输入新标签；重命名会级联更新所有任务与灵感中的同名标签；移除仅从词库删除，不影响已有数据；
3. **节点类型**：内置类型（开题/投稿截止/会议/中期检查/答辩/其他）可改名、不可删除；支持添加自定义类型（如 Rebuttal、基金申报、预答辩）；自定义类型未被引用时可删除，删除后引用它的节点回退为「其他」；新建/编辑节点时也可在类型下拉中直接新建；
4. 词汇库作为独立数据文件（`vocab.json`）存储于数据目录，随网盘同步。

**验收标准：**
- 在设置中新建节点类型后，新建节点弹窗的类型下拉立即可选；
- 重命名标签后，已打该标签的任务筛选与展示同步更新；
- 删除被引用的自定义类型时操作被阻止并提示引用数量。

## 7. 关键用户故事与验收标准

> 完整验收标准见第 6 节各功能，此处为跨模块的关键故事。

**US-1 项目启动**
> 作为一名研究生，我想新建一个项目并在 10 分钟内把导师布置的任务拆解录入，所以我能立刻看到这个项目的任务全貌和节奏。
- 验收：新建项目 → 录入 ≥5 个任务并设截止日 → 项目卡片显示正确进度，日历上出现对应截止日标记。

**US-2 Deadline 预警**
> 作为容易忘日期的人，我想打开应用就能看到所有临近节点的倒计时，所以不会错过任何投稿截止和学业节点。
- 验收：录入 3 个不同临近程度的节点（2 天 / 15 天 / 60 天）→ 今日概览分别显示红色 / 黄色 / 不显示（超出 30 天不进入预警区，但在节点页可见）。

**US-3 无摩擦速记**
> 作为正在读论文的人，我想不切换窗口就把想法记下来，所以灵感不会因为怕麻烦而流失。
- 验收：在浏览器全屏看论文时按 `Alt+N` → 弹出输入框 → 输入保存 → 回到浏览器，全程 < 5 秒且灵感在应用内可见。

**US-4 周报整理（V2）**
> 作为每周要向导师汇报的人，我想让应用自动汇总本周做的事生成草稿，所以写周报从 1 小时回忆变成 15 分钟润色。
- 验收：本周完成 8 个任务、写 4 条进展日志 → 生成草稿完整包含以上内容且按项目正确分组。

**US-5 多设备接力**
> 作为在两台电脑间切换工作的人，我想在宿舍电脑上录的任务第二天在实验室电脑上直接可见，所以不需要手动搬运数据。
- 验收：设备 A 录入数据 → 网盘同步完成后设备 B 打开应用 → 数据完整呈现（顺序使用，不做并发写入）。

## 8. 数据模型概要

> 详细的字段定义在开发阶段由技术方案确定，此处定义实体与关系，作为功能讨论的共识基础。

| 实体 | 关键字段 | 关系 |
| --- | --- | --- |
| Project 项目 | name, description, color, status, start_date, end_date | 1—N Task / Milestone / ProgressLog / Paper |
| Task 任务 | title, project_id?, status, priority, due_date?, tags, note, completed_at | N—1 Project（可空） |
| Milestone 节点 | title, date, type, project_id?, remind_days[], status | N—1 Project（可空，全局节点） |
| Idea 灵感 | content, tags, project_id?, status, created_at, converted_task_id? | 可关联 Task |
| ProgressLog 进展日志 | project_id, date, content(md) | N—1 Project |
| ToolBookmark 工具收藏 | name, type(url/file/folder/app), target, group, note | 独立 |
| Vocab 词汇库（V1.2） | tags[]{id, name}, milestone_types[]{id, name, builtin} | 标签被 Task/Idea 自由引用；节点类型被 Milestone 引用 |
| Paper 论文（V2） | title, venue, status, rounds, dates{}, repo_url, project_id? | N—1 Project；联动 Milestone / Task / Achievement |
| Achievement 成果（V2） | type, title, date, level, project_id?, detail | N—1 Project（可空） |
| Report 汇报（V2） | period, content(md), generated_at, edited | 聚合产物 |

## 9. 非功能需求

### 9.1 性能
1. 应用冷启动到今日概览可交互 ≤ 3 秒（普通固态硬盘办公电脑）；
2. 数据量按 3 年个人使用预估（≤ 5000 条任务/记录）时，所有列表与搜索操作响应 ≤ 0.5 秒。

### 9.2 易用性
1. 全中文界面；
2. 所有列表页支持键盘操作核心路径（回车保存、Esc 取消）；
3. 提供深色/浅色主题切换（跟随系统或手动）；
4. **布局自适应**（V1.2）：应用窗口在 880px 宽至全屏（含高分屏）范围内调整尺寸时，各功能页面正常展示——无横向滚动溢出、无内容遮挡，超长文字截断并保留悬浮提示；窄窗口下列/卡片区域单列堆叠，宽窗口下多列展开。

### 9.3 数据安全与可恢复性（重点）
1. **存储位置可配置**：首次启动引导用户选择数据目录，默认推荐坚果云同步目录（如 `…\Nutstore\1\格致科研工作台`），引导文案说明「数据将自动同步到坚果云」；
2. **文件写入原子性**：任何数据写入先写临时文件再替换，避免同步过程中产生半截文件；
3. **自动本地备份**：每次应用退出/每日首次运行时，将数据文件快照备份到数据目录下的 `backups/`，保留最近 30 份；
4. **删除保护**：删除项目等批量数据操作前，先自动备份；
5. **数据可迁移**：数据为明文 JSON 格式，用户可直接复制整个数据目录完成迁移或冷备份。

### 9.4 离线可用
全部功能离线可用，不依赖任何网络服务（网址收藏的图标抓取在线失败时降级为默认图标，不阻塞使用）。

### 9.5 兼容性
主要目标平台为 Windows 10/11 x64；采用跨平台框架实现，macOS/Linux 支持为顺带能力，不做专门测试。

## 10. 技术方案考虑

> 本节为推荐方案（供 AI 辅助开发启动时参考），最终实现允许在不违背第 9 节非功能需求的前提下调整。

### 10.1 推荐技术栈
- **框架**：Electron + React + TypeScript。理由：AI 辅助开发场景下资料与生态最成熟，全局快捷键（`globalShortcut`）、打开外部文件/网址（`shell` API）、托盘等桌面能力开箱即用；
- **UI**：Tailwind CSS + shadcn/ui（成熟组件多，样式质量有下限）；
- **构建打包**：Vite + electron-builder（Windows NSIS 安装包 + 便携版）；
- **备选说明**：Tauri 更轻量，但 Rust 后端在 AI 辅助开发与排查成本上不占优，故不推荐。

### 10.2 数据存储与网盘同步（关键设计）
1. **文件格式**：按实体集合存储为独立 JSON 文件（如 `projects.json`、`tasks.json`、`ideas.json`…），而非单文件数据库。理由：坚果云按文件粒度同步，改动只影响单个文件，冲突面最小，对坚果云免费版的每月流量额度也最友好；明文可直接检查与备份；
2. **写入策略**：应用内修改先缓存于内存，防抖（如停止编辑 1 秒后）批量落盘，减少网盘同步压力；
3. **同步冲突策略**：本产品为单人顺序使用（同一时间只在一台设备操作），采用「最后写入优先」；启动时检测文件修改时间，若发现数据文件比内存中记录更新（另一设备改过），自动重新加载；
4. **不做**：应用内置网络同步、实时协同——明确交给网盘。

### 10.3 与外部世界的边界
- 打开网址/文件/程序：一律调用系统默认行为（Electron `shell.openExternal` / `shell.openPath`），应用内不实现浏览器或文件管理器；
- 文献管理：不进入本产品，通过工具收藏提供 Zotero 等入口。

## 11. 成功指标

个人工具，采用轻量自定义指标，上线后观察 8 周：

| 类别 | 指标 | 目标 |
| --- | --- | --- |
| 采纳 | 上线 2 周内，所有在研项目（≥2 个）与全部活跃任务录入系统 | 100% |
| 习惯 | 连续 4 周每周使用 ≥ 5 天（应用启动日志统计） | 达成 |
| 价值-效率 | 周报初稿完成时间（V2 上线后） | ≤ 15 分钟 |
| 价值-安全 | 关键节点提前 3 天知晓率（回顾检查） | 100%，零被动逾期 |
| 可靠 | 数据丢失事件 | 0 起（含误删可恢复） |

## 12. 版本规划

| 版本 | 范围 | 建议节奏（业余时间开发） |
| --- | --- | --- |
| **V1.0 核心闭环** | 项目管理、任务列表、日历、时间节点、今日概览、灵感速记（快捷键 + 管理页）、工具收藏、项目进展日志、数据目录配置与备份 | 第 1–2 周框架与项目/任务；第 3–4 周日历/节点/概览/灵感；第 5 周工具箱与打磨；第 6 周打包安装与双机同步验证 |
| **V1.x 打磨** | 使用反馈修正、深色模式、模板自定义 | 按需 |
| **V2.0 产出增强** | 看板、论文投稿跟踪、周报/月报自动生成、成果台账、汇报导出 | V1 稳定使用 4 周后启动 |

里程碑验收：每个版本完成后，按第 6 节验收标准逐条验收；V1 验收必须包含「双机同步接力」实测（US-5）。

## 13. 风险与对策

| 风险 | 概率/影响 | 对策 |
| --- | --- | --- |
| 范围过大导致烂尾 | 高 / 高 | 严格执行 V1/V2 分期；V1 只做核心闭环；新想法一律先进 Backlog（第 15 节）不进当期范围 |
| 网盘同步异常导致数据冲突或丢失 | 中 / 高 | 文件级 JSON + 原子写入 + 每日自动备份 30 份 + 最后写入优先；文档明确「一次只用一台设备」使用约定 |
| 用了两周后弃用（习惯难养成） | 中 / 高 | 今日概览做默认启动页保证每日接触；快捷键速记降低录入成本；V2 周报自动生成提供「离不开」的正向回报 |
| AI 辅助开发质量不稳定 | 中 / 中 | 每个功能模块有明确验收标准，可逐条验收；模块化推进，单模块不合格不阻塞其他模块 |
| Electron 应用包体积大、内存占用高 | 低 / 低 | 个人使用可接受；不为此更换技术栈 |

## 14. 依赖与假设

1. 用户拥有并正常使用坚果云（免费版即可满足，文件级 JSON 同步的流量消耗极小）；
2. 使用设备为 Windows 10/11 电脑，至少两台；
3. 开发由 AI 辅助完成，产品所有者负责需求澄清与验收，开发周期按业余时间投入估算；
4. 假设单人顺序使用（不同时在两台设备编辑），并发编辑保护不在范围内。

## 15. 范围外（Out of Scope）

见 3.2 非目标。补充明确排除：数据看板/统计分析（如时间投入统计）、番茄钟等专注工具、存量数据导入工具（无存量数据，见决策 D3）。

**后续候选（Backlog，不承诺排期）**：日历 ICS 订阅导出（导入手机/系统日历查看节点）、应用内 AI 辅助功能（如进展日志摘要）、时间投入统计看板。V2 完成且稳定使用后，从 Backlog 中按需挑选进入后续版本。

## 16. 决策记录（Decision Log）

以下事项已于 2026-08-21 评审全部议定，本 PRD 当前无未决问题：

| # | 事项 | 决策 | 落实位置 |
| --- | --- | --- | --- |
| D1 | 文献阅读管理是否纳入 | **排除**。文献管理使用 Zotero 等专业工具，本应用通过工具收藏提供入口，并预置示例收藏 | 3.2、6.7、10.3 |
| D2 | 同步网盘选型 | **坚果云**（免费版即可）。首次启动引导默认指向坚果云同步目录 | 9.3、10.2、14 |
| D3 | 存量数据导入 | **无存量数据**，不开发导入工具 | 15 |
| D4 | 应用命名 | 定名**「格致」（Gezhi）**，取自「格物致知」——近代对科学（Science）的旧译；全称「格致 · 科研工作台 / Gezhi Research Workbench」，开发标识符 `gezhi-workbench` | 文档全局 |
| D5 | 日历 ICS 订阅导出 | 移入**后续候选（Backlog）**，V2 完成后按需挑选 | 15 |
| D6 | 任务「预估耗时」字段 | **不增加**。任务已含截止日期 + 优先级，时间约束表达充分，避免录入负担 | 8 |
| D7 | 标签与节点类型是否可自定义 | **可自定义**（V1.2）。新增词汇库（vocab.json）：标签支持库管理 + 录入联想 + 重命名级联；节点类型支持自定义（内置类型可改名不可删、被引用的自定义类型不可删）。同时明确窗口布局自适应要求（880px–全屏） | 6.3、6.5、6.11、8、9.2 |

---

## 附录 A：术语表

| 术语 | 定义 |
| --- | --- |
| 项目（Project） | 一个研究方向/课题/论文对应的长期工作单元 |
| 任务（Task） | 可执行的具体待办，可属于某个项目或为杂项 |
| 节点（Milestone） | 日历上的关键日期事件，如开题、投稿截止 |
| 进展日志（ProgressLog） | 项目下按日期记录的工作过程描述 |
| 灵感（Idea） | 快速捕捉的想法、假设、待验证思路 |
| 成果台账（Achievement） | 论文/专利/获奖等成果的结构化清单（V2） |

## 附录 B：修订记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| V1.0 | 2026-08-20 | 初稿：三轮需求沟通后形成完整 PRD |
| V1.1 | 2026-08-21 | 评审定稿：解决全部 6 项遗留问题并记入决策记录（第 16 节）；应用定名「格致」；新增工具收藏预置示例收藏（6.7）；同步方案明确为坚果云 |
| V1.2 | 2026-08-21 | 开发中需求变更：新增词汇库管理（标签库 + 自定义节点类型，6.11，记入决策 D7）；明确布局自适应非功能需求（9.2） |
